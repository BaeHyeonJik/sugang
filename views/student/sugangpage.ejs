<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>교수의 강좌</title>
</head>
<body>
    <form action="/back" method="post">
        <input type="hidden" name="where" value='homepage'>
        <input type="submit" value="뒤로가기">
    </form>
    <p><%=name%> <%=num%></p>
    <table border="1">
        <thead>
            <tr>
                <th>교수</th>
                <th>강의명</th>
                <th>학점</th>
                <th>현재인원/총원</th>
                <th>수강신청</th>
            </tr>
        </thead>
        <tbody>
            <% for (let lecture of lectures) { %>
                <tr>
                    <td><%= lecture.name %></td>
                    <td><%= lecture.title %></td>
                    <td><%= lecture.credit %></td>
                    <td><%= lecture.st_count %>/<%= lecture.total %></td>
                    <td>
                        <form action="/sugang" method="post">
                            <input type="hidden" name="p_id" value=<%= lecture.id %>>
                            <input type="hidden" name="title" value=<%= lecture.title %>>
                            <input type="hidden" name="st_count" value=<%= lecture.st_count %>>
                            <input type="hidden" name="total" value=<%= lecture.total %>>
                            <input type="submit" value="수강신청">
                        </form>
                    </td>
                </tr>
            <% } %>
        </tbody>
    </table>
    <%= message %>
</body>
</html>